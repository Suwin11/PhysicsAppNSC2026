=== button/ : เฟรมปุ่ม ✅ engine เขียน text ทับ (เว้นกลางให้โล่ง) ===

เฟรมพื้นใช้ทุกปุ่ม (3 สถานะ):
  frame_normal.png    frame_hover.png    frame_pressed.png

ปุ่ม OK/CANCEL (ไม่มี = ใช้เฟรมพื้น):
  frame_ok.png    frame_cancel.png

ปุ่มสวิตช์ ON/OFF:
  frame_toggle_on.png    frame_toggle_off.png

ปุ่มล็อก (เลือกวาดแยกทุกปุ่ม) -> ใส่ในโฟลเดอร์ lock/ (ดู example_theme/button/lock/)

** ไม่มีเฟรม = ปุ่มใช้สไตล์สี default ของ dark/light **
